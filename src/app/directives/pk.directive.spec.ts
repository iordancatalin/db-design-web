import { PkDirective } from './pk.directive';

describe('PkDirective', () => {
  it('should create an instance', () => {
    const directive = new PkDirective();
    expect(directive).toBeTruthy();
  });
});
